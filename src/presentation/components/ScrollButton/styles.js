import styled from 'styled-components';

export const ScrollButtonContainer = styled.img`
    visibility: hidden;
    opacity: 0;
    position: fixed;
    height: min(10%, 2rem);
    right: min(2rem, 5%);
    top: 55vh;
    cursor: pointer;
`;