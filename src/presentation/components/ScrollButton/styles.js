import styled from 'styled-components';

export const ScrollButtonContainer = styled.img`
    opacity: 1;
    position: fixed;
    height: min(10%, 2rem);
    right: min(2rem, 5%);
    top: 55vh;
    cursor: pointer;
    .showButton{
        opacity: 1;
    }
`;